/**
 * Collage route (US3.7, T088). A standalone mode reachable from the home screen;
 * the interactive surface is the client CollageScreen.
 */
import { CollageScreen } from '@/modules/collage/CollageScreen';

export default function CollagePage() {
  return <CollageScreen />;
}
